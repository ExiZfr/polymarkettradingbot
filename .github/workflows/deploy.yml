name: 🚀 Auto Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔐 Deploy to VPS via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script_stop: false
          debug: true
          script: |
            set -x
            set -e
            export PATH=$PATH:/usr/local/bin
            
            echo "========================================"
            echo "🚀 STARTING AUTO-DEPLOYMENT"
            echo "========================================"
            
            # Debug Info
            echo "👤 User: $(whoami)"
            echo "🏠 Home: $HOME"
            echo "📂 PWD: $(pwd)"
            
            # Application Directory (Use $HOME instead of ~)
            APP_DIR="$HOME/PolygraalX"
            echo "🎯 Target: $APP_DIR"
            
            # Check if directory exists
            if [ -d "$APP_DIR" ]; then
              echo "✅ Directory exists. Navigating..."
              cd "$APP_DIR" || exit 1
              
              echo "📥 [1/5] Syncing with GitHub..."
              # Trust GitHub host
              mkdir -p ~/.ssh
              ssh-keyscan -H github.com >> ~/.ssh/known_hosts 2>/dev/null || true
              
              # Reset any local changes to avoid conflicts
              git fetch origin main
              git reset --hard origin/main
            else
              echo "⚠️ Directory not found. Cloning repository..."
              
              # Trust GitHub host
              mkdir -p ~/.ssh
              ssh-keyscan -H github.com >> ~/.ssh/known_hosts 2>/dev/null || true
              
              # Ensure parent dir exists (unlikely needed for home but good practice)
              cd "$HOME"
              git clone git@github.com:ExiZfr/PolygraalX.git PolygraalX
              cd "$APP_DIR" || exit 1
            fi
            
            echo "📦 [2/5] Installing dependencies..."
            npm install
            
            echo "🗄️ [3/5] Syncing database..."
            npx prisma generate
            npx prisma db push
            
            echo "🏗️ [4/5] Building Next.js app..."
            npm run build
            
            echo "♻️ [5/5] Restarting Application..."
            # Check if pm2 is installed
            if ! command -v pm2 &> /dev/null; then
                echo "❌ PM2 not found. Installing global pm2..."
                npm install -g pm2
            fi

            # Start or Restart using PM2
            if pm2 describe polygraalx > /dev/null 2>&1; then
                echo "🔄 Restarting existing process..."
                pm2 restart polygraalx
            else
                echo "▶️ Starting new process..."
                pm2 start npm --name polygraalx -- start
            fi
            
            echo "========================================"
            echo "✅ DEPLOYMENT SUCCESSFUL"
            echo "========================================"
