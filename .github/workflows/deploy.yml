name: 🚀 Auto Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔐 Deploy to VPS via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script_stop: true
          debug: true
          script: |
            echo "========================================"
            echo "🚀 STARTING AUTO-DEPLOYMENT"
            echo "========================================"
            
            cd ~/polymarkettradingbot
            
            echo "📥 [1/5] Syncing with GitHub (force)..."
            # Force sync - discard any local changes on VPS
            git fetch origin main
            git reset --hard origin/main
            
            echo "📦 [2/5] Installing dependencies..."
            npm install
            
            echo "🗄️ [3/5] Syncing database..."
            npx prisma generate
            npx prisma db push
            
            echo "🏗️ [4/5] Building Next.js app..."
            npm run build
            
            echo "♻️ [5/5] Restarting Application..."
            pm2 restart polymarket-dashboard || pm2 start npm --name polymarket-dashboard -- start
            
            echo "========================================"
            echo "✅ DEPLOYMENT SUCCESSFUL"
            echo "========================================"
